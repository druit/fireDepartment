import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firedepartment.app',
  appName: 'fire-department',
  webDir: 'dist/fire-department',
  bundledWebRuntime: false
};

export default config;
